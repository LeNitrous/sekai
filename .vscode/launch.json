{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Example",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "Build Examples",
            "program": "${workspaceFolder}/examples/${input:LaunchExample}/bin/Debug/net7.0/${input:LaunchExample}.dll",
            "args": [],
            "cwd": "${workspaceFolder}/examples/${input:LaunchExample}",
            "console": "internalConsole",
            "stopAtEntry": false
        }
    ],
    "inputs": [
        {
            "type": "pickString",
            "id": "LaunchExample",
            "description": "Select Example",
            "options": [
                "Triangle"
            ]
        }
    ]
}